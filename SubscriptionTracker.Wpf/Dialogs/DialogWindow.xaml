<Window x:Class="SubscriptionTracker.Wpf.Dialogs.DialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding DialogTitle}"
        Width="480"
        MinWidth="420"
        MaxWidth="560"
        SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ShowInTaskbar="False"
        AllowsTransparency="True"
        Background="Transparent">
  <Border Background="{DynamicResource AppCardBrush}"
          BorderBrush="{DynamicResource AppBorderBrush}"
          BorderThickness="1"
          CornerRadius="24"
          Padding="24">
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>

      <Grid Grid.Row="0">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Width="44"
                Height="44"
                Background="{Binding AccentBrush}"
                CornerRadius="22">
          <TextBlock HorizontalAlignment="Center"
                     VerticalAlignment="Center"
                     FontSize="22"
                     FontWeight="Bold"
                     Foreground="White"
                     Text="{Binding KindGlyph}" />
        </Border>

        <StackPanel Grid.Column="1" Margin="16,0,0,0" VerticalAlignment="Center">
          <TextBlock FontSize="21"
                     FontWeight="SemiBold"
                     Text="{Binding DialogTitle}" />
          <TextBlock Margin="0,4,0,0"
                     Foreground="{DynamicResource AppMutedBrush}"
                     Text="{Binding KindCaption}" />
        </StackPanel>
      </Grid>

      <TextBlock Grid.Row="1"
                 Margin="0,18,0,0"
                 FontSize="14"
                 Foreground="{DynamicResource AppForegroundBrush}"
                 TextWrapping="Wrap"
                 Text="{Binding DialogMessage}" />

      <StackPanel Grid.Row="2"
                  Margin="0,24,0,0"
                  HorizontalAlignment="Right"
                  Orientation="Horizontal">
        <Button MinWidth="104"
                Margin="0,0,10,0"
                Style="{Binding SecondaryButtonStyle}"
                Visibility="{Binding SecondaryButtonVisibility}"
                IsCancel="True"
                Click="SecondaryClick"
                Content="{Binding SecondaryButtonText}" />
        <Button MinWidth="104"
                Style="{Binding PrimaryButtonStyle}"
                IsDefault="True"
                Click="PrimaryClick"
                Content="{Binding PrimaryButtonText}" />
      </StackPanel>
    </Grid>
  </Border>
</Window>
