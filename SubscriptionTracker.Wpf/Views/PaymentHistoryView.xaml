<UserControl x:Class="SubscriptionTracker.Wpf.Views.PaymentHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <UserControl.Resources>
    <Style x:Key="HistoryListItemStyle" TargetType="ListBoxItem">
      <Setter Property="HorizontalContentAlignment" Value="Stretch" />
      <Setter Property="Padding" Value="0" />
      <Setter Property="Margin" Value="0,0,0,12" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ListBoxItem">
            <Border x:Name="Root"
                    Background="{DynamicResource AppCardBrush}"
                    BorderBrush="{DynamicResource AppBorderBrush}"
                    BorderThickness="1"
                    CornerRadius="16"
                    Padding="16">
              <ContentPresenter />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Root" Property="BorderBrush" Value="{DynamicResource AppAccentAltBrush}" />
              </Trigger>
              <Trigger Property="IsSelected" Value="True">
                <Setter TargetName="Root" Property="BorderBrush" Value="{DynamicResource AppAccentBrush}" />
                <Setter TargetName="Root" Property="Background" Value="{DynamicResource AppSecondaryCardBrush}" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </UserControl.Resources>

  <DockPanel>
    <Border DockPanel.Dock="Top" Style="{StaticResource CardBorderStyle}">
      <StackPanel>
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="220" />
            <ColumnDefinition Width="180" />
          </Grid.ColumnDefinitions>

          <StackPanel Grid.Column="0">
            <TextBlock FontSize="22" FontWeight="SemiBold" Text="{DynamicResource HistoryTitle}" />
            <TextBlock Margin="0,4,0,0"
                       Foreground="{DynamicResource AppMutedBrush}"
                       Text="История запланированных, оплаченных и пропущенных платежей." />
          </StackPanel>

          <TextBox Grid.Column="1"
                   Margin="12,0,12,0"
                   VerticalContentAlignment="Center"
                   Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />

          <ComboBox Grid.Column="2"
                    ItemsSource="{Binding StatusFilters}"
                    SelectedItem="{Binding SelectedStatusFilter}" />
        </Grid>

        <UniformGrid Margin="0,16,0,0" Columns="3">
          <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,12,0" Padding="14">
            <StackPanel>
              <TextBlock Foreground="{DynamicResource AppMutedBrush}" Text="В выборке" />
              <TextBlock Margin="0,6,0,0" FontSize="22" FontWeight="Bold" Text="{Binding VisibleCount}" />
            </StackPanel>
          </Border>
          <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,12,0" Padding="14">
            <StackPanel>
              <TextBlock Foreground="{DynamicResource AppMutedBrush}" Text="Оплачено" />
              <TextBlock Margin="0,6,0,0" FontSize="22" FontWeight="Bold" Text="{Binding PaidCount}" />
            </StackPanel>
          </Border>
          <Border Style="{StaticResource CardBorderStyle}" Padding="14">
            <StackPanel>
              <TextBlock Foreground="{DynamicResource AppMutedBrush}" Text="Запланировано" />
              <TextBlock Margin="0,6,0,0" FontSize="22" FontWeight="Bold" Text="{Binding PlannedCount}" />
            </StackPanel>
          </Border>
        </UniformGrid>
      </StackPanel>
    </Border>

    <Border Style="{StaticResource CardBorderStyle}">
      <ListBox ItemsSource="{Binding ItemsView}"
               Background="Transparent"
               BorderThickness="0"
               ItemContainerStyle="{StaticResource HistoryListItemStyle}">
        <ListBox.ItemTemplate>
          <DataTemplate>
            <Grid>
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="140" />
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="120" />
                <ColumnDefinition Width="130" />
                <ColumnDefinition Width="2*" />
              </Grid.ColumnDefinitions>

              <StackPanel Grid.Column="0">
                <TextBlock Foreground="{DynamicResource AppMutedBrush}" Text="Дата" />
                <TextBlock Margin="0,4,0,0" FontWeight="SemiBold" Text="{Binding PaymentDateLabel}" />
              </StackPanel>

              <StackPanel Grid.Column="1" Margin="16,0,16,0">
                <TextBlock Foreground="{DynamicResource AppMutedBrush}" Text="Подписка" />
                <TextBlock Margin="0,4,0,0" FontSize="16" FontWeight="SemiBold" Text="{Binding SubscriptionName}" />
              </StackPanel>

              <StackPanel Grid.Column="2">
                <TextBlock Foreground="{DynamicResource AppMutedBrush}" Text="Сумма" />
                <TextBlock Margin="0,4,0,0" FontWeight="SemiBold" Text="{Binding AmountLabel}" />
              </StackPanel>

              <StackPanel Grid.Column="3">
                <TextBlock Foreground="{DynamicResource AppMutedBrush}" Text="Статус" />
                <Border Margin="0,4,0,0"
                        HorizontalAlignment="Left"
                        Style="{StaticResource StatusBadgeBorderStyle}"
                        Background="{Binding StatusColorHex, Converter={StaticResource HexToBrushConverter}}">
                  <TextBlock Style="{StaticResource StatusBadgeTextStyle}" Text="{Binding StatusLabel}" />
                </Border>
              </StackPanel>

              <StackPanel Grid.Column="4">
                <TextBlock Foreground="{DynamicResource AppMutedBrush}" Text="Комментарий" />
                <TextBlock Margin="0,4,0,0" TextWrapping="Wrap" Text="{Binding NoteLabel}" />
              </StackPanel>
            </Grid>
          </DataTemplate>
        </ListBox.ItemTemplate>
      </ListBox>
    </Border>
  </DockPanel>
</UserControl>
