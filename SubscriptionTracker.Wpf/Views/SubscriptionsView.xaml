<UserControl x:Class="SubscriptionTracker.Wpf.Views.SubscriptionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <UserControl.Resources>
    <Style x:Key="SubscriptionListItemStyle" TargetType="ListBoxItem">
      <Setter Property="HorizontalContentAlignment" Value="Stretch" />
      <Setter Property="Padding" Value="0" />
      <Setter Property="Margin" Value="0,0,0,12" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ListBoxItem">
            <Border x:Name="Root"
                    Background="{DynamicResource AppCardBrush}"
                    BorderBrush="{DynamicResource AppBorderBrush}"
                    BorderThickness="1"
                    CornerRadius="16"
                    Padding="16">
              <ContentPresenter />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Root" Property="BorderBrush" Value="{DynamicResource AppAccentAltBrush}" />
              </Trigger>
              <Trigger Property="IsSelected" Value="True">
                <Setter TargetName="Root" Property="BorderBrush" Value="{DynamicResource AppAccentBrush}" />
                <Setter TargetName="Root" Property="Background" Value="{DynamicResource AppSecondaryCardBrush}" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </UserControl.Resources>

  <DockPanel>
    <Border DockPanel.Dock="Top" Style="{StaticResource CardBorderStyle}">
      <StackPanel>
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="180" />
            <ColumnDefinition Width="180" />
          </Grid.ColumnDefinitions>

          <StackPanel Grid.Column="0">
            <TextBlock FontSize="22" FontWeight="SemiBold" Text="{DynamicResource SubscriptionsTitle}" />
            <TextBlock Margin="0,4,0,0"
                       Foreground="{DynamicResource AppMutedBrush}"
                       Text="Рабочий список подписок с быстрыми действиями и фильтрацией." />
          </StackPanel>

          <TextBox Grid.Column="1"
                   Margin="12,0,12,0"
                   VerticalContentAlignment="Center"
                   Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />

          <ComboBox Grid.Column="2"
                    ItemsSource="{Binding Filters}"
                    SelectedItem="{Binding SelectedFilter}" />
        </Grid>

        <UniformGrid Margin="0,16,0,0" Columns="3">
          <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,12,0" Padding="14">
            <StackPanel>
              <TextBlock Foreground="{DynamicResource AppMutedBrush}" Text="В списке" />
              <TextBlock Margin="0,6,0,0" FontSize="22" FontWeight="Bold" Text="{Binding VisibleCount}" />
            </StackPanel>
          </Border>
          <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,12,0" Padding="14">
            <StackPanel>
              <TextBlock Foreground="{DynamicResource AppMutedBrush}" Text="Активных" />
              <TextBlock Margin="0,6,0,0" FontSize="22" FontWeight="Bold" Text="{Binding ActiveCount}" />
            </StackPanel>
          </Border>
          <Border Style="{StaticResource CardBorderStyle}" Padding="14">
            <StackPanel>
              <TextBlock Foreground="{DynamicResource AppMutedBrush}" Text="Месячная нагрузка" />
              <TextBlock Margin="0,6,0,0" FontSize="22" FontWeight="Bold" Text="{Binding VisibleMonthlyTotalLabel}" />
            </StackPanel>
          </Border>
        </UniformGrid>

        <WrapPanel Margin="0,16,0,0">
          <Button Style="{StaticResource PrimaryButtonStyle}" Command="{Binding AddCommand}" Content="Добавить" />
          <Button Style="{StaticResource SecondaryButtonStyle}" Command="{Binding EditCommand}" Content="Редактировать" />
          <Button Style="{StaticResource NeutralButtonStyle}" Command="{Binding MarkPaidCommand}" Content="Отметить как оплачено" />
          <Button Style="{StaticResource NeutralButtonStyle}" Command="{Binding SkipCommand}" Content="Пропустить" />
          <Button Style="{StaticResource SecondaryButtonStyle}" Command="{Binding ToggleActiveCommand}" Content="Вкл/выкл" />
          <Button Style="{StaticResource SecondaryButtonStyle}" Command="{Binding ExportCommand}" Content="Экспорт в Excel" />
          <Button Style="{StaticResource DangerButtonStyle}" Command="{Binding DeleteCommand}" Content="Удалить" />
        </WrapPanel>
      </StackPanel>
    </Border>

    <Border Style="{StaticResource CardBorderStyle}">
      <ListBox ItemsSource="{Binding ItemsView}"
               SelectedItem="{Binding SelectedItem}"
               Background="Transparent"
               BorderThickness="0"
               ItemContainerStyle="{StaticResource SubscriptionListItemStyle}">
        <ListBox.ItemTemplate>
          <DataTemplate>
            <Grid>
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2.2*" />
                <ColumnDefinition Width="1.2*" />
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="1.1*" />
                <ColumnDefinition Width="1.1*" />
              </Grid.ColumnDefinitions>

              <StackPanel Grid.Column="0" Margin="0,0,18,0">
                <TextBlock FontSize="18" FontWeight="SemiBold" Text="{Binding Name}" />
                <TextBlock Margin="0,4,0,0" Foreground="{DynamicResource AppMutedBrush}" Text="{Binding Description}" />
              </StackPanel>

              <StackPanel Grid.Column="1">
                <TextBlock Foreground="{DynamicResource AppMutedBrush}" Text="Категория" />
                <TextBlock Margin="0,4,0,0" FontWeight="SemiBold" Text="{Binding CategoryName}" />
                <TextBlock Margin="0,10,0,0" Foreground="{DynamicResource AppMutedBrush}" Text="Период" />
                <TextBlock Margin="0,4,0,0" Text="{Binding CycleLabel}" />
              </StackPanel>

              <StackPanel Grid.Column="2">
                <TextBlock Foreground="{DynamicResource AppMutedBrush}" Text="Сумма" />
                <TextBlock Margin="0,4,0,0" FontWeight="SemiBold" Text="{Binding AmountLabel}" />
                <TextBlock Margin="0,10,0,0" Foreground="{DynamicResource AppMutedBrush}" Text="В месяц" />
                <TextBlock Margin="0,4,0,0" Text="{Binding MonthlyCostLabel}" />
              </StackPanel>

              <StackPanel Grid.Column="3">
                <TextBlock Foreground="{DynamicResource AppMutedBrush}" Text="Следующее списание" />
                <TextBlock Margin="0,4,0,0" FontWeight="SemiBold" Text="{Binding NextPaymentLabel}" />
                <TextBlock Margin="0,10,0,0" Foreground="{DynamicResource AppMutedBrush}" Text="Статус срока" />
                <TextBlock Margin="0,4,0,0" Text="{Binding CountdownLabel}" />
              </StackPanel>

              <StackPanel Grid.Column="4" HorizontalAlignment="Right">
                <TextBlock Foreground="{DynamicResource AppMutedBrush}" HorizontalAlignment="Right" Text="Состояние" />
                <Border Margin="0,4,0,0"
                        Style="{StaticResource StatusBadgeBorderStyle}"
                        Background="{Binding StatusColorHex, Converter={StaticResource HexToBrushConverter}}">
                  <TextBlock Style="{StaticResource StatusBadgeTextStyle}" Text="{Binding StatusBadgeLabel}" />
                </Border>
              </StackPanel>
            </Grid>
          </DataTemplate>
        </ListBox.ItemTemplate>
      </ListBox>
    </Border>
  </DockPanel>
</UserControl>
