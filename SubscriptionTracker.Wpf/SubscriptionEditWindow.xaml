<Window x:Class="SubscriptionTracker.Wpf.SubscriptionEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding WindowTitle}"
        Width="620"
        Height="720"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
  <Border Background="{DynamicResource AppBackgroundBrush}" Padding="24">
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="*" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>

      <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel>
          <TextBlock FontSize="14" Foreground="{DynamicResource AppMutedBrush}" Text="{DynamicResource SubscriptionEditorName}" />
          <TextBox Margin="0,6,0,14" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />

          <TextBlock FontSize="14" Foreground="{DynamicResource AppMutedBrush}" Text="{DynamicResource SubscriptionEditorDescription}" />
          <TextBox Margin="0,6,0,14"
                   Height="70"
                   AcceptsReturn="True"
                   TextWrapping="Wrap"
                   VerticalContentAlignment="Top"
                   Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" />

          <Grid Margin="0,0,0,14">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Margin="0,0,10,0">
              <TextBlock FontSize="14" Foreground="{DynamicResource AppMutedBrush}" Text="{DynamicResource SubscriptionEditorCategory}" />
              <ComboBox Margin="0,6,0,14"
                        DisplayMemberPath="Name"
                        SelectedValuePath="Id"
                        ItemsSource="{Binding Categories}"
                        SelectedValue="{Binding SelectedCategoryId}" />

              <TextBlock FontSize="14" Foreground="{DynamicResource AppMutedBrush}" Text="{DynamicResource SubscriptionEditorBillingCycle}" />
              <ComboBox Margin="0,6,0,0"
                        ItemsSource="{Binding BillingCycles}"
                        SelectedItem="{Binding SelectedBillingCycle}">
                <ComboBox.ItemTemplate>
                  <DataTemplate>
                    <TextBlock Text="{Binding Converter={StaticResource BillingCycleToLabelConverter}}" />
                  </DataTemplate>
                </ComboBox.ItemTemplate>
              </ComboBox>
            </StackPanel>

            <StackPanel Grid.Column="1" Margin="10,0,0,0">
              <TextBlock FontSize="14" Foreground="{DynamicResource AppMutedBrush}" Text="{DynamicResource SubscriptionEditorAmount}" />
              <TextBox Margin="0,6,0,14" Text="{Binding Amount, UpdateSourceTrigger=PropertyChanged}" />

              <TextBlock FontSize="14" Foreground="{DynamicResource AppMutedBrush}" Text="{DynamicResource SubscriptionEditorCurrency}" />
              <ComboBox Margin="0,6,0,0"
                        ItemsSource="{Binding Currencies}"
                        SelectedItem="{Binding SelectedCurrency}" />
            </StackPanel>
          </Grid>

          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Margin="0,0,10,0">
              <TextBlock FontSize="14" Foreground="{DynamicResource AppMutedBrush}" Text="{DynamicResource SubscriptionEditorFirstPaymentDate}" />
              <DatePicker Margin="0,6,0,14" SelectedDate="{Binding FirstPaymentDate}" />

              <TextBlock FontSize="14" Foreground="{DynamicResource AppMutedBrush}" Text="{DynamicResource SubscriptionEditorReminderDays}" />
              <TextBox Margin="0,6,0,14" Text="{Binding ReminderDaysBefore, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>

            <StackPanel Grid.Column="1" Margin="10,0,0,0">
              <TextBlock FontSize="14" Foreground="{DynamicResource AppMutedBrush}" Text="{DynamicResource SubscriptionEditorNextPaymentDate}" />
              <DatePicker Margin="0,6,0,14" SelectedDate="{Binding NextPaymentDate}" />

              <CheckBox Margin="0,18,0,10" IsChecked="{Binding AutoRenewal}" Content="{DynamicResource SubscriptionEditorAutoRenewal}" />
              <CheckBox Margin="0,0,0,10" IsChecked="{Binding IsLowUsage}" Content="{DynamicResource SubscriptionEditorLowUsage}" />
              <CheckBox IsChecked="{Binding IsActive}" Content="{DynamicResource SubscriptionEditorIsActive}" />
            </StackPanel>
          </Grid>
        </StackPanel>
      </ScrollViewer>

      <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
        <Button Width="120" Click="SaveClick" Style="{StaticResource PrimaryButtonStyle}" Content="{DynamicResource SubscriptionEditorSave}" />
        <Button Width="120" IsCancel="True" Content="{DynamicResource SubscriptionEditorCancel}" />
      </StackPanel>
    </Grid>
  </Border>
</Window>
