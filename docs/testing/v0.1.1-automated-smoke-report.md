# v0.1.1 Automated Smoke Report

## Covered Automatically

- solution build
- unit and scenario test suite
- release publish
- isolated first-run startup
- SQLite database creation
- default category and sample subscription seeding
- reminder query on seeded data
- Excel export file generation
- backup and restore workflow on a temporary database

## Current Automated Commands

```powershell
dotnet build SubscriptionTracker.sln
dotnet test SubscriptionTracker.Tests\SubscriptionTracker.Tests.csproj --no-build
dotnet publish SubscriptionTracker.Wpf\SubscriptionTracker.Wpf.csproj -c Release -o artifacts\publish\wpf
```

## Isolated Startup Result

Validated against the published WPF executable with:

- `SUBSCRIPTION_TRACKER_APPDATA_DIR` pointing to a temporary directory inside the workspace

Observed result:

- the application process starts successfully
- `subscription_tracker.db` is created
- SQLite sidecar files are created as expected
- `settings.json` is not created until the user explicitly saves settings

## Still Manual

- visual rendering of all screens
- creating/editing a subscription through the UI
- mark as paid / skip / disable / re-enable from the UI
- filters on calendar and history through the UI
- theme/language switching visuals
- custom dialog appearance
- toast delivery on the current machine
